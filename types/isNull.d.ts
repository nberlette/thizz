/**
 * Returns `true` if value is `null`; otherwise, returns `false`.
 * @param {*} value - target value to inspect
 * @returns {boolean} `boolean`
 */
declare module 'thizz/isNull' {
  export function isNull(value: any): boolean;
  export default isNull;
}
